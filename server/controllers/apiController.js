//now in node.js not express
const fs = require('fs');
const path = require('path');
const fetch = require('node-fetch');

const apiController = {};

apiController.solve = (req, res, next) => {
};

module.exports = apiController;